// OpenID inbound Identity flow with hello.coop for social logins
Flow agama.inbound.oidc.hello
     Basepath ""
     Configs hello_op_conf
// launch utility flow
obj = Trigger agama.inbound.oauth2.authzCodeWithUserInfo oidc
// log
Log "@info Process to onboard user"
// check authentication 
When obj.success is true
     // grab response data
     obj.data = obj.data.profile
     // User provisioning and grab user uid
     uid = Call agama.inbound.UserOnBoardHelper#exec obj.data uidPrefix
     // return user id for successful authentication
     it_pddso = { success: true, data: { userId: uid } }
     Finish it_pddso
// fail attempt
it_frxgg = { success: false, error: "Authentication failed, please try again later." }
Finish it_frxgg